# Data Model

The SQLAlchemy model layer contains all MVP entities from the brief, using UUID string primary keys, UTC timestamps, soft deletion fields, explicit enums, JSON columns for structured outputs, and indexes on common lookup fields.
