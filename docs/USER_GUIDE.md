# User Guide

1. Run `python -m scripts.tasks seed`.
2. Start the API with `python -m uvicorn app.api.main:app --reload`.
3. Start the dashboard with `python -m streamlit run dashboard/Home.py`.
4. Review seeded profile facts in the Review Queue before using them in finalized CVs.
