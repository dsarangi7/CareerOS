# Privacy

CareerOS is local-first. The default implementation does not call external APIs or send personal data outside the machine.

Generated CVs, job descriptions, communications, interview prep packs, weekly reports, and exports stay on the local filesystem/database unless the user explicitly copies or submits them elsewhere.

The optional OpenAI agent adapter is disabled by default. When enabled through environment configuration, agent inputs should be treated as shared with the configured provider.
